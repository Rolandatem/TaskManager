<UserControl x:Class="IncStores.TaskManager.WpfTaskViewer.Views.Main.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IncStores.TaskManager.WpfTaskViewer.ViewModels.Main"
             xmlns:cc="clr-namespace:IncStores.TaskManager.WpfTaskViewer.CustomControls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:LoginViewModelDesign, IsDesignTimeCreatable=True}"
             FontFamily="{StaticResource Roboto}">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <cc:ContentSection Width="300">
            <StackPanel>
                <TextBlock Text="USER NAME:" />
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0" />

                <TextBlock Text="PASSWORD:" Margin="0,10,0,0" />
                <PasswordBox x:Name="passwordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,2,0,0">
                    <PasswordBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding LoginCommand}" />
                    </PasswordBox.InputBindings>
                </PasswordBox>

                <TextBlock Text="{Binding LoginMessage}" FontWeight="Bold" Foreground="#FFE00101" Margin="0,5,0,0" Style="{DynamicResource CollapsableTextBlock}" />

                <Button Content="Login" Margin="0,10,0,0" HorizontalAlignment="Right" Command="{Binding LoginCommand}" />
            </StackPanel>            
        </cc:ContentSection>
    </Grid>
</UserControl>
