<UserControl x:Class="IncStores.TaskManager.WpfTaskViewer.Views.Main.DisconnectedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Background="{StaticResource Global.AppWorkspace.Background}"
             Foreground="{StaticResource Global.Text.Foreground}"
             FontFamily="{StaticResource Roboto}">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <TextBlock Text="Not Connected to SignalR Server." FontWeight="Bold" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>

</UserControl>
