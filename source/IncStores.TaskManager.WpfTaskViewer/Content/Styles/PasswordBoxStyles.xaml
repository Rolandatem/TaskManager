<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- BRUSHES -->
    <SolidColorBrush x:Key="Global.Passwordbox.Background" Color="{StaticResource Global.Main.Light.Color}" />
    <SolidColorBrush x:Key="Global.Passwordbox.Foreground" Color="{StaticResource Global.Main.Darker.Color}" />

    <!-- COMMON GLOBAL -->
    <Style TargetType="PasswordBox">
        <Setter Property="Height" Value="25" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="5,0,0,0" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource Global.Passwordbox.Foreground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Border CornerRadius="5" BorderThickness="1" BorderBrush="#FFABADB3" Background="{StaticResource Global.Passwordbox.Background}">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>