<UserControl x:Class="IncStores.TaskManager.WpfTaskViewer.Views.Monitors.LiveStatusMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IncStores.TaskManager.WpfTaskViewer.ViewModels.Monitors"
             xmlns:cc="clr-namespace:IncStores.TaskManager.WpfTaskViewer.CustomControls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:LiveStatusMonitorViewModelDesign, IsDesignTimeCreatable=True}"
             Background="{StaticResource Global.AppWorkspace.Background}"
             FontFamily="{StaticResource Roboto}"
             d:Height="400" d:Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" MinWidth="200" />
        </Grid.ColumnDefinitions>
        
        <!-- GRID SPLITTERS -->
        <cc:GripGridSplitter Grid.Row="2" Grid.Column="0" Style="{StaticResource HorizontalGripGridSplitter}" Margin="20,3" Padding="0,1" />
        <cc:GripGridSplitter Grid.Row="1" Grid.Column="1" Grid.RowSpan="3" Style="{StaticResource VerticalGripGridSplitter}" Margin="5" Padding="1,0" />

        <!-- TITLE -->
        <cc:ViewTitle Grid.Row="0" Grid.ColumnSpan="3" Title="Live Status Monitor" />

        <!-- AUDIT HISTORY -->
        <cc:ContentSection Header="Audit History" Grid.Row="1" Grid.Column="0" Margin="0,10,0,0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2,0,0" DockPanel.Dock="Top">
                    <TextBlock Text="Filter by Group Key:" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding SharedHubCommunicator.AuditLogGroupKeys, Mode=OneWay}" SelectedItem="{Binding SharedHubCommunicator.SelectedFilterGroupKey}" Margin="5,0,0,0" />
                </StackPanel>
                <DataGrid ItemsSource="{Binding SharedHubCommunicator.FilteredAuditLog, Mode=OneWay}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" HeadersVisibility="Column" HorizontalScrollBarVisibility="Auto" Margin="0,5,0,0" DockPanel.Dock="Top">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding AuditDateTime}" />
                        <DataGridTextColumn Header="Group Key" Binding="{Binding GroupKey}" />
                        <DataGridTextColumn Header="Initiator" Binding="{Binding Initiator}" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </cc:ContentSection>

        <!-- ERROR LOG -->
        <cc:ContentSection Header="Error Log" Grid.Row="3" Grid.Column="0" Margin="0">
            <DockPanel Margin="0,5,0,0">
                <StackPanel Orientation="Horizontal" Margin="0,2,0,0" DockPanel.Dock="Top">
                    <CheckBox Content="Information" IsChecked="{Binding SharedHubCommunicator.ShowInformation}" />
                    <CheckBox Content="Debug" IsChecked="{Binding SharedHubCommunicator.ShowDebug}" Margin="10,0,0,0" />
                    <CheckBox Content="Warning" IsChecked="{Binding SharedHubCommunicator.ShowWarning}" Margin="10,0,0,0" />
                    <CheckBox Content="Critical" IsChecked="{Binding SharedHubCommunicator.ShowCritical}" Margin="10,0,0,0" />
                    <CheckBox Content="Error" IsChecked="{Binding SharedHubCommunicator.ShowError}" Margin="10,0,0,0" />
                </StackPanel>
                <DataGrid ItemsSource="{Binding SharedHubCommunicator.FilteredErrorLog, Mode=OneWay}" IsReadOnly="True" CanUserDeleteRows="False" CanUserAddRows="False" Margin="0,5,0,0" DockPanel.Dock="Top" AutoGenerateColumns="False" HorizontalScrollBarVisibility="Auto" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Level" Binding="{Binding LogLevel}" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </cc:ContentSection>

        <!-- RECIPE PROGRESS -->
        <cc:RecipeProgress Grid.Row="1" Grid.Column="2" Grid.RowSpan="3" RecipeProgressSource="{Binding SharedHubCommunicator.RecipeWorkerStatuses}" Margin="0,10,0,0" />

    </Grid>
    
</UserControl>
