<UserControl x:Class="IncStores.TaskManager.WpfTaskViewer.Views.TaskRecipe.TaskRecipeMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IncStores.TaskManager.WpfTaskViewer.ViewModels.TaskRecipe"
             xmlns:cc="clr-namespace:IncStores.TaskManager.WpfTaskViewer.CustomControls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:TaskRecipeMainViewModelDesign, IsDesignTimeCreatable=True}" 
             Background="{StaticResource Global.AppWorkspace.Background}"
             FontFamily="{StaticResource Roboto}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <cc:ViewTitle Title="Task Recipe Type Editor" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add" Command="{Binding AddRecipeTypeCommand}" />
                <Button Content="Refresh" Command="{Binding RefreshRecipeTypesCommand}" Margin="10,0,0,0" />
            </StackPanel>
        </Grid>

        <cc:ContentSection Grid.Row="1" Margin="0,10,0,0">
            <DataGrid ItemsSource="{Binding TaskRecipeList}" SelectedItem="{Binding SelectedTaskRecipeType}" MinWidth="500" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditRecipeTypeCommand}" />
                    <KeyBinding Key="Delete" Command="{Binding DeleteRecipeTypeCommand}" />
                </DataGrid.InputBindings>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Edit" Command="{Binding EditRecipeTypeCommand}" />
                        <MenuItem Header="Delete" Command="{Binding DeleteRecipeTypeCommand}" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="String Key" Binding="{Binding StringKey}" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Fault Emails" Binding="{Binding EmailNotificationList}" />
                    <DataGridTextColumn Header="Fault SMS" Binding="{Binding SMSNotificationListRaw}" />
                </DataGrid.Columns>
            </DataGrid>
        </cc:ContentSection>
    </Grid>
</UserControl>
